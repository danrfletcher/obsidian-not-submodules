{
	"name": "not-submodules",
	"version": "1.1.2",
	"description": "Replace git submodules with plain nested git repos, discovered by scanning your vault and kept out of its history by a self-maintaining git hook.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "tsx --test \"src/**/*.test.ts\""
	},
	"keywords": [],
	"author": "Dan Fletcher",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"esbuild": "0.21.5",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"tsx": "^4.23.15",
		"typescript": "4.7.4"
	}
}
